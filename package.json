{
  "name": "google-gog-mcp",
  "private": true,
  "version": "1.0.0",
  "description": "MCP server bridging Google Drive/Docs/Sheets read access for Poke",
  "main": "dist/server.js",
  "bin": {
    "poke-google-workspace-mcp": "dist/server.js"
  },
  "scripts": {
    "build:mcp": "tsc -p tsconfig.mcp.json",
    "start": "node dist/server.js",
    "start:http": "node dist/server.js --http",
    "dev": "tsx src/server.ts",
    "dev:http": "tsx src/server.ts --http",
    "typecheck": "tsc -p tsconfig.mcp.json --noEmit",
    "validate": "tsx src/validate.ts",
    "lint": "tsc -p tsconfig.mcp.json --noEmit"
  },
  "keywords": ["mcp", "google-workspace", "google-drive", "poke", "ai"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "googleapis": "^171.4.0",
    "open": "^11.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3"
  }
}
